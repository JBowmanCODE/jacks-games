import FootballGame from "./FootballGame";

export const metadata = {
  title: "Football Top Trumps — Jack's Games",
  description: "Compare your player's stats against the opponent — who has the better numbers?",
};

export default function FootballPage() {
  return <FootballGame />;
}
