import SudokuGame from "./SudokuGame";

export const metadata = {
  title: "Sudoku — Jack's Games",
  description: "Sudoku puzzles for all ages — Easy (8+), Medium (10+), Hard (16+)",
};

export default function SudokuPage() {
  return <SudokuGame />;
}
