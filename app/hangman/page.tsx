import HangmanGame from "./HangmanGame";

export const metadata = {
  title: "Hangman — Jack's Games",
  description: "Guess the word before it's too late — challenging vocabulary for 15+",
};

export default function HangmanPage() {
  return <HangmanGame />;
}
